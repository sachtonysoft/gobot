package main

import (
	"master"
	
	"Sachtony"
	"Sachtonysoft"
	"SachtonyStore"
	"TonyDuong"
	"Merchants"
	"AdminCenter"
	"PartnerCenter"
	"Developers"
	
)

func main() {

	ToDo := connect(import)
	ToDo.string()
	var name type[RegularExpression]
	if listen=true 
		{
			var Action type(queries)
			type queries struct {
				ID     		int
				Project    	string
				Table []	bool
			}
			return=Message
			
		}
	if listen=false
		{
			var Action type(null)
			return=Message
		}
}